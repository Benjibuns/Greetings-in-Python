import math

"""
Tools:
- math library
- sorted function
- list slicing
- computations
"""


def sorting_hat(sale_prices):
    return:
        halved_items = (
            math.ceil(len(sorted(sale_prices)) / 2)
        )


sale_prices = [
    100,
    83,
    220,
    40,
    100,
    400,
    10,
    1,
    3
]


print(math.ceil(len(sale_prices) / 2))
